[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run build && cp -r ../../skills ./skills"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
